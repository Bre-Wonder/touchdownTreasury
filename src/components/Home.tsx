import React from 'react';
import Hero from './Hero';
import ProductGrid from './ProductGrid';

const Home: React.FC = () => {
  return (
    <main>
      <Hero />
      <ProductGrid />
    </main>
  );
};

export default Home;
